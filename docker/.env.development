# Development Environment Variables

# App Environment
APP_ENV=development

# SMTP Configuration (smtp4dev for local development)
SMTP_ENABLED=true
SMTP_HOST=smtp4dev
SMTP_PORT=25
SMTP_USERNAME=
SMTP_PASSWORD=