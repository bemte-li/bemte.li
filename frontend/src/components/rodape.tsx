import Image from 'next/image';
import React from 'react';

const Rodape: React.FC = () => {
    return (
        <div className="flex flex-col bg-gray-500 max-w-screen-sm mx-auto">
            <h3>Rodapé temporário</h3>
            <p>A definir</p>
        </div>
    );
};

export default Rodape;
